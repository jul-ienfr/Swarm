import {
  PREDICTION_MARKETS_BASELINE_MODEL,
  PREDICTION_MARKETS_SCHEMA_VERSION,
  capitalLedgerSnapshotSchema,
  crossVenueMarketRefSchema,
  crossVenueMatchSchema,
  marketRegimeSchema,
  decisionPacketSchema,
  executionIntentPreviewSchema,
  evidencePacketSchema,
  latencyReferenceBundleSchema,
  forecastPacketSchema,
  marketDescriptorSchema,
  marketRecommendationPacketSchema,
  predictionMarketArtifactRefSchema,
  predictionMarketPacketBundleSchema,
  marketSnapshotSchema,
  predictionMarketBudgetsSchema,
  predictionMarketRunSummarySchema,
  predictionMarketsAdviceRequestSchema,
  predictionMarketsReplayRequestSchema,
  resolutionAnomalyReportSchema,
  resolutionPolicySchema,
  runManifestSchema,
  strategyCandidatePacketSchema,
  strategyDecisionPacketSchema,
  strategyShadowReportSchema,
  strategyShadowSummarySchema,
  autonomousAgentReportSchema,
  basketIntentPreviewSchema,
  quotePairIntentPreviewSchema,
  tradeIntentSchema,
  venueCapabilitiesSchema,
  venueHealthSnapshotSchema,
} from './schemas'

export type PredictionMarketContractFieldDoc = {
  field: string
  type: string
  description: string
}

export type PredictionMarketContractDoc = {
  schema: string
  purpose: string
  required_fields: PredictionMarketContractFieldDoc[]
}

function requiredField(field: string, type: string, description: string): PredictionMarketContractFieldDoc {
  return {
    field,
    type,
    description,
  }
}

const CAPTURED_AT = '2026-04-08T00:00:00.000Z'
const EVALUATED_AT = '2026-04-08T00:00:05.000Z'
const PRODUCED_AT = '2026-04-08T00:00:08.000Z'
const STARTED_AT = '2026-04-08T00:00:10.000Z'
const COMPLETED_AT = '2026-04-08T00:00:40.000Z'
const NEXT_REVIEW_AT = '2026-04-08T00:15:00.000Z'

const marketDescriptorExample = marketDescriptorSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  venue: 'polymarket',
  venue_type: 'execution-equivalent',
  market_id: 'pm-2028-us-presidency-dem-win',
  event_id: 'event-2028-us-presidency',
  condition_id: 'condition-2028-us-presidency',
  question_id: 'question-2028-us-presidency-dem-win',
  slug: 'will-the-democratic-nominee-win-the-2028-us-presidential-election',
  question: 'Will the Democratic nominee win the 2028 US presidential election?',
  description: 'Canonical binary market descriptor example for prediction-markets contracts.',
  outcomes: ['Yes', 'No'],
  outcome_token_ids: ['token-yes-001', 'token-no-001'],
  start_at: '2026-04-01T00:00:00.000Z',
  end_at: '2028-11-07T23:59:59.000Z',
  active: true,
  closed: false,
  accepting_orders: true,
  restricted: false,
  liquidity_usd: 250000,
  volume_usd: 1250000,
  volume_24h_usd: 45000,
  best_bid: 0.47,
  best_ask: 0.49,
  last_trade_price: 0.48,
  tick_size: 0.01,
  min_order_size: 1,
  is_binary_yes_no: true,
  source_urls: ['https://example.com/polymarket/markets/pm-2028-us-presidency-dem-win'],
})

const resolutionPolicyExample = resolutionPolicySchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  market_id: marketDescriptorExample.market_id,
  venue: marketDescriptorExample.venue,
  status: 'eligible',
  manual_review_required: false,
  reasons: [],
  primary_sources: ['https://example.com/polymarket/rules/2028-us-presidency'],
  resolution_text: 'Resolved to Yes if the Democratic nominee wins the certified 2028 US presidential election.',
  evaluated_at: EVALUATED_AT,
})

const marketSnapshotExample = marketSnapshotSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  venue: marketDescriptorExample.venue,
  market: marketDescriptorExample,
  captured_at: CAPTURED_AT,
  yes_outcome_index: 0,
  yes_token_id: 'token-yes-001',
  yes_price: 0.48,
  no_price: 0.52,
  midpoint_yes: 0.48,
  best_bid_yes: 0.47,
  best_ask_yes: 0.49,
  spread_bps: 200,
  book: {
    token_id: 'token-yes-001',
    market_condition_id: marketDescriptorExample.condition_id,
    fetched_at: CAPTURED_AT,
    best_bid: 0.47,
    best_ask: 0.49,
    last_trade_price: 0.48,
    tick_size: 0.01,
    min_order_size: 1,
    bids: [
      { price: 0.47, size: 1200 },
      { price: 0.46, size: 1800 },
    ],
    asks: [
      { price: 0.49, size: 950 },
      { price: 0.5, size: 2100 },
    ],
    depth_near_touch: 2150,
  },
  history: [
    { timestamp: 1775605200000, price: 0.45 },
    { timestamp: 1775606100000, price: 0.47 },
    { timestamp: 1775606400000, price: 0.48 },
  ],
  source_urls: ['https://example.com/polymarket/markets/pm-2028-us-presidency-dem-win/snapshot'],
})

const evidencePacketExample = evidencePacketSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  evidence_id: 'evidence-2026-04-08-001',
  market_id: marketDescriptorExample.market_id,
  venue: marketDescriptorExample.venue,
  type: 'market_data',
  title: 'Order book and midpoint snapshot',
  summary: 'Midpoint is 0.48 with a 200 bps spread and healthy near-touch depth.',
  source_url: 'https://example.com/polymarket/markets/pm-2028-us-presidency-dem-win/snapshot',
  captured_at: CAPTURED_AT,
  content_hash: 'sha256-market-data-example-001',
  metadata: {
    liquidity_bucket: 'high',
    feed: 'rest',
  },
})

const forecastPacketExample = forecastPacketSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  packet_version: '1.0.0',
  packet_kind: 'forecast',
  compatibility_mode: 'market_only',
  market_only_compatible: true,
  contract_id: `${PREDICTION_MARKETS_SCHEMA_VERSION}:forecast:1.0.0:market_only`,
  source_bundle_id: 'pm-run-2026-04-08-001:advisor_packet_bundle',
  source_packet_refs: [evidencePacketExample.evidence_id],
  social_context_refs: ['social:deliberation:2026-04-08-001'],
  market_context_refs: [marketDescriptorExample.market_id, marketDescriptorExample.slug],
  market_id: marketDescriptorExample.market_id,
  venue: marketDescriptorExample.venue,
  basis: 'market_midpoint',
  model: PREDICTION_MARKETS_BASELINE_MODEL,
  probability_yes: 0.54,
  confidence: 0.66,
  rationale: 'Forecast is modestly above market midpoint after incorporating the current thesis and evidence packet.',
  evidence_refs: [evidencePacketExample.evidence_id],
  produced_at: PRODUCED_AT,
})

const marketRecommendationPacketExample = marketRecommendationPacketSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  packet_version: '1.0.0',
  packet_kind: 'recommendation',
  compatibility_mode: 'market_only',
  market_only_compatible: true,
  contract_id: `${PREDICTION_MARKETS_SCHEMA_VERSION}:recommendation:1.0.0:market_only`,
  source_bundle_id: 'pm-run-2026-04-08-001:advisor_packet_bundle',
  source_packet_refs: ['pm-run-2026-04-08-001:forecast_packet'],
  social_context_refs: ['social:deliberation:2026-04-08-001'],
  market_context_refs: [marketDescriptorExample.market_id, marketDescriptorExample.slug],
  market_id: marketDescriptorExample.market_id,
  venue: marketDescriptorExample.venue,
  action: 'wait',
  side: null,
  confidence: 0.62,
  fair_value_yes: forecastPacketExample.probability_yes,
  market_price_yes: marketSnapshotExample.yes_price,
  market_bid_yes: marketSnapshotExample.best_bid_yes,
  market_ask_yes: marketSnapshotExample.best_ask_yes,
  edge_bps: 600,
  spread_bps: marketSnapshotExample.spread_bps,
  reasons: ['Fair value is above midpoint, but not enough to justify crossing the spread.'],
  risk_flags: ['manual-review-before-live'],
  rationale: 'The signal is positive, but execution quality remains marginal at the current spread.',
  why_now: ['Liquidity is present and the market is active.'],
  why_not_now: ['Spread remains wide relative to the current edge estimate.'],
  watch_conditions: ['Re-evaluate if midpoint falls below 0.46 or spread tightens under 100 bps.'],
  resolution_policy_ref: `${marketDescriptorExample.market_id}:resolution_policy`,
  comparable_market_refs: ['kalshi:KXUSPRES28-DEM-WIN'],
  requires_manual_review: true,
  next_review_at: NEXT_REVIEW_AT,
  produced_at: PRODUCED_AT,
})

const decisionPacketExample = decisionPacketSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  packet_version: '1.0.0',
  packet_kind: 'decision',
  compatibility_mode: 'market_only',
  market_only_compatible: true,
  contract_id: `${PREDICTION_MARKETS_SCHEMA_VERSION}:decision:1.0.0:market_only`,
  source_bundle_id: 'pm-run-2026-04-08-001:advisor_packet_bundle',
  source_packet_refs: ['pm-run-2026-04-08-001:forecast_packet', 'pm-run-2026-04-08-001:recommendation_packet'],
  social_context_refs: ['social:deliberation:2026-04-08-001'],
  market_context_refs: [marketDescriptorExample.market_id, marketDescriptorExample.slug],
  correlation_id: 'decision-2026-04-08-001',
  question: marketDescriptorExample.question,
  topic: 'us-politics',
  objective: 'Produce a reusable decision packet that can drive forecast and recommendation generation.',
  probability_estimate: 0.54,
  confidence_band: {
    low: 0.5,
    high: 0.58,
  },
  scenarios: [
    {
      scenario_id: 'base-case',
      label: 'Base case',
      summary: 'The race remains close and the market stays liquid enough for monitoring.',
      probability: 0.64,
    },
  ],
  risks: [
    {
      risk_id: 'macro-shift',
      label: 'Macro polling shock',
      severity: 'high',
      summary: 'A major macro event could move polling and invalidate the thesis quickly.',
    },
  ],
  recommendation: 'Wait for either a better entry price or stronger thesis confirmation.',
  rationale_summary: 'Thesis leans yes, but the current market structure does not yet support a confident trade.',
  artifacts: [
    {
      artifact_id: `artifact:${forecastPacketExample.market_id}:forecast`,
      artifact_type: 'forecast_packet',
      uri: 'https://example.com/prediction-markets/forecast-packet.json',
      sha256: 'sha256-forecast-example-001',
    },
  ],
  mode_used: 'deliberation_massive',
  engine_used: 'gpt-5.4',
  runtime_used: 'swarm',
  resolution_policy_ref: `${marketDescriptorExample.market_id}:resolution_policy`,
  comparable_market_refs: ['kalshi:KXUSPRES28-DEM-WIN'],
  requires_manual_review: true,
})

const crossVenueMarketRefExample = crossVenueMarketRefSchema.parse({
  venue: 'polymarket',
  market_id: marketDescriptorExample.market_id,
  venue_type: 'execution-equivalent',
  slug: marketDescriptorExample.slug,
  question: marketDescriptorExample.question,
  side: 'yes',
})

const crossVenueMatchExample = crossVenueMatchSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  canonical_event_id: 'event:us-presidency-2028:democratic-nominee-win',
  left_market_ref: crossVenueMarketRefExample,
  right_market_ref: {
    venue: 'kalshi',
    market_id: 'KXUSPRES28-DEM-WIN',
    venue_type: 'execution-equivalent',
    slug: 'democratic-nominee-win-2028',
    question: 'Will the Democratic nominee win the 2028 U.S. presidential election?',
    side: 'yes',
  },
  semantic_similarity_score: 0.96,
  resolution_compatibility_score: 0.94,
  payout_compatibility_score: 0.9,
  currency_compatibility_score: 1,
  manual_review_required: false,
  notes: ['Both markets are binary yes/no contracts about the same election outcome.'],
})

const venueCapabilitiesExample = venueCapabilitiesSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  venue: 'kalshi',
  venue_type: 'execution-equivalent',
  supports_discovery: true,
  supports_metadata: true,
  supports_orderbook: true,
  supports_trades: true,
  supports_positions: true,
  supports_execution: true,
  supports_websocket: true,
  supports_paper_mode: false,
  rate_limit_notes: 'Respect venue throttling and back off when order book polling is bursty.',
  automation_constraints: ['manual review before live'],
  last_verified_at: CAPTURED_AT,
})

const venueHealthSnapshotExample = venueHealthSnapshotSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  venue: 'kalshi',
  captured_at: CAPTURED_AT,
  health_score: 0.98,
  api_status: 'healthy',
  stream_status: 'healthy',
  staleness_ms: 1200,
  degraded_mode: 'normal',
  incident_flags: [],
  notes: 'Read-only data plane is healthy and within freshness budgets.',
})

const capitalLedgerSnapshotExample = capitalLedgerSnapshotSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  captured_at: CAPTURED_AT,
  venue: 'kalshi',
  cash_available: 1250,
  cash_locked: 250,
  collateral_currency: 'USD',
  open_exposure_usd: 600,
  withdrawable_amount: 900,
  transfer_latency_estimate_ms: 86400000,
})

const tradeIntentExample = tradeIntentSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  intent_id: 'intent-2026-04-08-001',
  run_id: 'pm-run-2026-04-08-001',
  venue: marketDescriptorExample.venue,
  market_id: marketDescriptorExample.market_id,
  side: 'yes',
  size_usd: 100,
  limit_price: 0.49,
  max_slippage_bps: 25,
  max_unhedged_leg_ms: 750,
  time_in_force: 'day',
  forecast_ref: `forecast:${forecastPacketExample.market_id}:${forecastPacketExample.produced_at}`,
  risk_checks_passed: true,
  created_at: PRODUCED_AT,
  notes: 'Canonical example intent for downstream execution or shadow simulation.',
})

const marketRegimeExample = marketRegimeSchema.parse({
  regime_id: 'regime-2026-04-08-001',
  label: 'stable_bullish',
  summary: 'Liquid market with a modest yes-side lean and an execution-sensitive spread.',
  confidence: 0.71,
  observed_at: CAPTURED_AT,
  signals: ['tightening mid', 'stable liquidity', 'execution friction'],
})

const latencyReferenceBundleExample = latencyReferenceBundleSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  bundle_id: 'latency-2026-04-08-001',
  run_id: 'pm-run-2026-04-08-001',
  venue: marketDescriptorExample.venue,
  market_id: marketDescriptorExample.market_id,
  captured_at: CAPTURED_AT,
  decision_latency_budget_ms: 5000,
  fetch_latency_budget_ms: 2000,
  snapshot_freshness_budget_ms: 30000,
  observed_latency_ms: 1200,
  p50_latency_ms: 900,
  p95_latency_ms: 1800,
  summary: 'Observed latencies remain inside the advisory budget.',
})

const quotePairIntentPreviewExample = quotePairIntentPreviewSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  preview_id: 'preview-quote-pair-2026-04-08-001',
  run_id: 'pm-run-2026-04-08-001',
  venue: marketDescriptorExample.venue,
  market_id: marketDescriptorExample.market_id,
  strategy_profile: 'balanced',
  strategy_family: 'directional',
  quotes: [
    { side: 'yes', price: 0.49, size_usd: 50 },
    { side: 'no', price: 0.51, size_usd: 50 },
  ],
  max_slippage_bps: 25,
  summary: 'Small two-sided quote pair used for a shadow execution preview.',
})

const basketIntentPreviewExample = basketIntentPreviewSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  preview_id: 'preview-basket-2026-04-08-001',
  run_id: 'pm-run-2026-04-08-001',
  venue: marketDescriptorExample.venue,
  basket_id: 'basket-2026-04-08-001',
  strategy_profile: 'balanced',
  strategy_family: 'cross_venue',
  legs: [
    {
      market_id: marketDescriptorExample.market_id,
      side: 'yes',
      price: 0.49,
      size_usd: 50,
    },
    {
      market_id: 'pm-2028-senate-control-dem',
      side: 'no',
      price: 0.53,
      size_usd: 35,
      notes: 'Hedge leg for the basket preview.',
    },
  ],
  max_slippage_bps: 40,
  summary: 'Two-leg basket preview for correlated exposure management.',
})

const executionIntentPreviewExample = executionIntentPreviewSchema.parse(quotePairIntentPreviewExample)

const resolutionAnomalyReportExample = resolutionAnomalyReportSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  report_id: 'resolution-anomaly-2026-04-08-001',
  run_id: 'pm-run-2026-04-08-001',
  venue: marketDescriptorExample.venue,
  market_id: marketDescriptorExample.market_id,
  anomaly_kind: 'policy_mismatch',
  severity: 'medium',
  detected_at: PRODUCED_AT,
  source_refs: [resolutionPolicyExample.market_id],
  impacted_artifact_refs: ['pm-run-2026-04-08-001:resolution_policy'],
  summary: 'One resolution note conflicts with the current shadow trading assumption.',
  notes: ['Carry the anomaly forward into the strategy decision report.'],
})

const autonomousAgentReportExample = autonomousAgentReportSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  report_id: 'agent-report-2026-04-08-001',
  agent_id: 'agent:strategy-001',
  agent_role: 'strategy-observer',
  run_id: 'pm-run-2026-04-08-001',
  venue: marketDescriptorExample.venue,
  market_id: marketDescriptorExample.market_id,
  strategy_profile: 'balanced',
  strategy_family: 'directional',
  market_regime: marketRegimeExample,
  generated_at: PRODUCED_AT,
  observations: ['Midpoint is still below the estimate.', 'Spread is wide enough to delay live action.'],
  actions: ['Hold shadow posture', 'Reassess after spread compression'],
  confidence: 0.72,
  summary: 'Agent supports waiting for a cleaner entry.',
})

const strategyShadowSummaryExample = strategyShadowSummarySchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  shadow_id: 'shadow-2026-04-08-001',
  run_id: 'pm-run-2026-04-08-001',
  venue: marketDescriptorExample.venue,
  market_id: marketDescriptorExample.market_id,
  strategy_profile: 'balanced',
  strategy_family: 'directional',
  candidate_count: 2,
  decision_count: 1,
  disagreement_count: 1,
  alignment_rate: 0.5,
  summary: 'The shadow run split on execution timing but agreed on the underlying thesis.',
})

const strategyShadowReportExample = strategyShadowReportSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  shadow_id: strategyShadowSummaryExample.shadow_id,
  report_id: 'shadow-report-2026-04-08-001',
  run_id: strategyShadowSummaryExample.run_id,
  venue: strategyShadowSummaryExample.venue,
  market_id: strategyShadowSummaryExample.market_id,
  strategy_profile: strategyShadowSummaryExample.strategy_profile,
  strategy_family: strategyShadowSummaryExample.strategy_family,
  candidate_count: strategyShadowSummaryExample.candidate_count,
  decision_count: strategyShadowSummaryExample.decision_count,
  disagreement_count: strategyShadowSummaryExample.disagreement_count,
  alignment_rate: strategyShadowSummaryExample.alignment_rate,
  summary: strategyShadowSummaryExample.summary,
  generated_at: COMPLETED_AT,
  candidate_refs: ['strategy-candidate-2026-04-08-001'],
  decision_refs: ['strategy-decision-2026-04-08-001'],
  outcome: 'aligned',
  notes: ['Shadow report captured for downstream audit and artifact replay.'],
})

const strategyCandidatePacketExample = strategyCandidatePacketSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  packet_version: '1.0.0',
  packet_kind: 'strategy_candidate',
  compatibility_mode: 'market_only',
  market_only_compatible: true,
  contract_id: `${PREDICTION_MARKETS_SCHEMA_VERSION}:strategy_candidate:1.0.0:market_only`,
  source_bundle_id: 'pm-run-2026-04-08-001:strategy_packet_bundle',
  source_packet_refs: [evidencePacketExample.evidence_id, forecastPacketExample.market_id],
  social_context_refs: ['social:deliberation:2026-04-08-001'],
  market_context_refs: [marketDescriptorExample.market_id],
  run_id: 'pm-run-2026-04-08-001',
  venue: marketDescriptorExample.venue,
  market_id: marketDescriptorExample.market_id,
  strategy_profile: 'balanced',
  market_regime: marketRegimeExample,
  correlation_id: 'strategy-candidate-2026-04-08-001',
  summary: 'Shadow candidate favors a small yes-side test with controlled slippage.',
  rationale: 'Edge is modest but still worth tracking in shadow mode.',
  candidate_id: 'strategy-candidate-2026-04-08-001',
  strategy_family: 'directional',
  confidence: 0.68,
  expected_edge_bps: 220,
  execution_intent_preview: executionIntentPreviewExample,
  latency_reference_bundle: latencyReferenceBundleExample,
  resolution_anomaly_report: resolutionAnomalyReportExample,
  autonomous_agent_report: autonomousAgentReportExample,
  shadow_summary: strategyShadowSummaryExample,
  created_at: PRODUCED_AT,
})

const strategyDecisionPacketExample = strategyDecisionPacketSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  packet_version: '1.0.0',
  packet_kind: 'strategy_decision',
  compatibility_mode: 'market_only',
  market_only_compatible: true,
  contract_id: `${PREDICTION_MARKETS_SCHEMA_VERSION}:strategy_decision:1.0.0:market_only`,
  source_bundle_id: 'pm-run-2026-04-08-001:strategy_packet_bundle',
  source_packet_refs: [strategyCandidatePacketExample.candidate_id],
  social_context_refs: ['social:deliberation:2026-04-08-001'],
  market_context_refs: [marketDescriptorExample.market_id],
  run_id: 'pm-run-2026-04-08-001',
  venue: marketDescriptorExample.venue,
  market_id: marketDescriptorExample.market_id,
  strategy_profile: 'balanced',
  market_regime: marketRegimeExample,
  correlation_id: 'strategy-decision-2026-04-08-001',
  summary: 'Keep the strategy in shadow mode until execution conditions improve.',
  rationale: 'The spread and the anomaly report both argue against live deployment.',
  decision_id: 'strategy-decision-2026-04-08-001',
  candidate_refs: [strategyCandidatePacketExample.candidate_id],
  selected_candidate_ref: strategyCandidatePacketExample.candidate_id,
  strategy_family: 'directional',
  decision: 'shadow',
  confidence: 0.74,
  execution_intent_preview: quotePairIntentPreviewExample,
  latency_reference_bundle: latencyReferenceBundleExample,
  resolution_anomaly_report: resolutionAnomalyReportExample,
  autonomous_agent_report: autonomousAgentReportExample,
  shadow_report: strategyShadowReportExample,
  created_at: COMPLETED_AT,
})

const predictionMarketPacketBundleExample = predictionMarketPacketBundleSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  bundle_id: 'pm-run-2026-04-08-001:packet_bundle',
  run_id: 'pm-run-2026-04-08-001',
  venue: marketDescriptorExample.venue,
  market_id: marketDescriptorExample.market_id,
  decision_packet: decisionPacketExample,
  strategy_candidate_packet: strategyCandidatePacketExample,
  strategy_decision_packet: strategyDecisionPacketExample,
  strategy_shadow_report: strategyShadowReportExample,
  evidence_packets: [evidencePacketExample],
  forecast_packet: forecastPacketExample,
  recommendation_packet: marketRecommendationPacketExample,
  metadata: {
    strategy_profile: 'balanced',
  },
})

const predictionMarketArtifactRefExample = predictionMarketArtifactRefSchema.parse({
  artifact_id: 'pm-run-2026-04-08-001:market_snapshot',
  artifact_type: 'market_snapshot',
  sha256: 'sha256-market-snapshot-example-001',
  layout_version: 'v1',
  bucket: 'runs',
  file_name: 'market_snapshot.json',
  run_key: 'prediction-markets/v1/runs/pm-run-2026-04-08-001/runs/market_snapshot.json',
  market_key: `prediction-markets/v1/venues/${marketDescriptorExample.venue}/markets/${marketDescriptorExample.market_id}/runs/pm-run-2026-04-08-001--market_snapshot.json`,
  latest_key: `prediction-markets/v1/venues/${marketDescriptorExample.venue}/markets/${marketDescriptorExample.market_id}/runs/latest--market_snapshot.json`,
})

const runManifestExample = runManifestSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  run_id: 'pm-run-2026-04-08-001',
  mode: 'advise',
  venue: marketDescriptorExample.venue,
  market_id: marketDescriptorExample.market_id,
  market_slug: marketDescriptorExample.slug,
  actor: 'system',
  started_at: STARTED_AT,
  completed_at: COMPLETED_AT,
  status: 'completed',
  config_hash: 'cfg-prediction-markets-example-001',
  artifact_refs: [
    {
      artifact_id: 'pm-run-2026-04-08-001:market_descriptor',
      artifact_type: 'market_descriptor',
      sha256: 'sha256-market-descriptor-example-001',
      layout_version: 'v1',
      bucket: 'catalog',
      file_name: 'market_descriptor.json',
      run_key: 'prediction-markets/v1/runs/pm-run-2026-04-08-001/catalog/market_descriptor.json',
      market_key: `prediction-markets/v1/venues/${marketDescriptorExample.venue}/markets/${marketDescriptorExample.market_id}/catalog/pm-run-2026-04-08-001--market_descriptor.json`,
      latest_key: `prediction-markets/v1/venues/${marketDescriptorExample.venue}/markets/${marketDescriptorExample.market_id}/catalog/latest--market_descriptor.json`,
    },
    predictionMarketArtifactRefExample,
    {
      artifact_id: 'pm-run-2026-04-08-001:forecast_packet',
      artifact_type: 'forecast_packet',
      sha256: 'sha256-forecast-packet-example-001',
      layout_version: 'v1',
      bucket: 'runs',
      file_name: 'forecast_packet.json',
      run_key: 'prediction-markets/v1/runs/pm-run-2026-04-08-001/runs/forecast_packet.json',
      market_key: `prediction-markets/v1/venues/${marketDescriptorExample.venue}/markets/${marketDescriptorExample.market_id}/runs/pm-run-2026-04-08-001--forecast_packet.json`,
      latest_key: `prediction-markets/v1/venues/${marketDescriptorExample.venue}/markets/${marketDescriptorExample.market_id}/runs/latest--forecast_packet.json`,
    },
    {
      artifact_id: 'pm-run-2026-04-08-001:run_manifest',
      artifact_type: 'run_manifest',
      sha256: 'sha256-run-manifest-example-001',
      layout_version: 'v1',
      bucket: 'runs',
      file_name: 'run_manifest.json',
      run_key: 'prediction-markets/v1/runs/pm-run-2026-04-08-001/runs/run_manifest.json',
      market_key: `prediction-markets/v1/venues/${marketDescriptorExample.venue}/markets/${marketDescriptorExample.market_id}/runs/pm-run-2026-04-08-001--run_manifest.json`,
      latest_key: `prediction-markets/v1/venues/${marketDescriptorExample.venue}/markets/${marketDescriptorExample.market_id}/runs/latest--run_manifest.json`,
    },
  ],
})

const predictionMarketRunSummaryExample = predictionMarketRunSummarySchema.parse({
  run_id: runManifestExample.run_id,
  source_run_id: null,
  workspace_id: 1,
  venue: runManifestExample.venue,
  mode: runManifestExample.mode,
  market_id: runManifestExample.market_id,
  market_slug: runManifestExample.market_slug ?? null,
  status: 'completed',
  recommendation: marketRecommendationPacketExample.action,
  side: marketRecommendationPacketExample.side,
  confidence: marketRecommendationPacketExample.confidence,
  probability_yes: forecastPacketExample.probability_yes,
  market_price_yes: marketRecommendationPacketExample.market_price_yes,
  edge_bps: marketRecommendationPacketExample.edge_bps,
  created_at: 1775606410,
  updated_at: 1775606440,
  manifest: runManifestExample,
  artifact_refs: runManifestExample.artifact_refs,
})

const predictionMarketBudgetsExample = predictionMarketBudgetsSchema.parse({
  schema_version: PREDICTION_MARKETS_SCHEMA_VERSION,
  fetch_latency_budget_ms: 2000,
  snapshot_freshness_budget_ms: 30000,
  decision_latency_budget_ms: 5000,
  stream_reconnect_budget_ms: 10000,
  cache_ttl_ms: 5000,
  max_retries: 2,
  backpressure_policy: 'degrade-to-wait',
})

const predictionMarketsAdviceRequestExample = predictionMarketsAdviceRequestSchema.parse({
  venue: marketDescriptorExample.venue,
  market_id: marketDescriptorExample.market_id,
  strategy_profile: 'hybrid',
  enabled_strategy_families: ['directional', 'cross_venue'],
  thesis_probability: forecastPacketExample.probability_yes,
  thesis_rationale: 'Deliberation packet suggests a slight yes edge, but spread remains the main execution limiter.',
  min_edge_bps: 150,
  max_spread_bps: 400,
  history_limit: 100,
  research_signals: [
    {
      source: 'deliberation_massive',
      correlation_id: decisionPacketExample.correlation_id,
      thesis_strength: 'moderate',
    },
  ],
})

const predictionMarketsReplayRequestExample = predictionMarketsReplayRequestSchema.parse({
  run_id: runManifestExample.run_id,
})

export const predictionMarketContractExamples = {
  decisionPacket: decisionPacketExample,
  predictionMarketBudgets: predictionMarketBudgetsExample,
  marketDescriptor: marketDescriptorExample,
  resolutionPolicy: resolutionPolicyExample,
  marketSnapshot: marketSnapshotExample,
  evidencePacket: evidencePacketExample,
  crossVenueMarketRef: crossVenueMarketRefExample,
  crossVenueMatch: crossVenueMatchExample,
  venueCapabilities: venueCapabilitiesExample,
  venueHealthSnapshot: venueHealthSnapshotExample,
  capitalLedgerSnapshot: capitalLedgerSnapshotExample,
  tradeIntent: tradeIntentExample,
  marketRegime: marketRegimeExample,
  latencyReferenceBundle: latencyReferenceBundleExample,
  quotePairIntentPreview: quotePairIntentPreviewExample,
  basketIntentPreview: basketIntentPreviewExample,
  executionIntentPreview: executionIntentPreviewExample,
  resolutionAnomalyReport: resolutionAnomalyReportExample,
  autonomousAgentReport: autonomousAgentReportExample,
  strategyShadowSummary: strategyShadowSummaryExample,
  strategyShadowReport: strategyShadowReportExample,
  strategyCandidatePacket: strategyCandidatePacketExample,
  strategyDecisionPacket: strategyDecisionPacketExample,
  predictionMarketPacketBundle: predictionMarketPacketBundleExample,
  forecastPacket: forecastPacketExample,
  marketRecommendationPacket: marketRecommendationPacketExample,
  predictionMarketArtifactRef: predictionMarketArtifactRefExample,
  runManifest: runManifestExample,
  predictionMarketRunSummary: predictionMarketRunSummaryExample,
  predictionMarketsAdviceRequest: predictionMarketsAdviceRequestExample,
  predictionMarketsReplayRequest: predictionMarketsReplayRequestExample,
}

export type PredictionMarketContractExampleName = keyof typeof predictionMarketContractExamples

export const predictionMarketContractExampleNames = Object.keys(
  predictionMarketContractExamples,
) as PredictionMarketContractExampleName[]

export const predictionMarketContractDocs: Record<
  PredictionMarketContractExampleName,
  PredictionMarketContractDoc
> = {
  decisionPacket: {
    schema: 'decisionPacketSchema',
    purpose: 'Minimal deliberation output that can feed forecast and recommendation generation.',
    required_fields: [
      requiredField('correlation_id', 'string', 'Stable ID tying the packet back to the upstream decision run.'),
      requiredField('question', 'string', 'Human-readable market question evaluated by the packet.'),
      requiredField('topic', 'string', 'Routing or grouping key for the decision context.'),
      requiredField('objective', 'string', 'What the packet is trying to decide or recommend.'),
      requiredField('probability_estimate', 'number 0..1', 'Single-point probability estimate.'),
      requiredField('confidence_band', '{ low, high }', 'Bounded uncertainty interval around the estimate.'),
      requiredField('recommendation', 'string', 'Actionable recommendation text.'),
      requiredField('rationale_summary', 'string', 'Short explanation of the recommendation.'),
      requiredField('mode_used', 'string', 'Name of the upstream reasoning mode.'),
      requiredField('engine_used', 'string', 'Model or engine identifier used to create the packet.'),
      requiredField('runtime_used', 'string', 'Runtime or system that executed the packet.'),
    ],
  },
  predictionMarketBudgets: {
    schema: 'predictionMarketBudgetsSchema',
    purpose: 'Operational performance and freshness budgets for read-only or execution-capable flows.',
    required_fields: [
      requiredField('snapshot_freshness_budget_ms', 'integer', 'Maximum tolerated age for a usable snapshot.'),
      requiredField('decision_latency_budget_ms', 'integer', 'Maximum end-to-end latency allowed for a decision.'),
    ],
  },
  marketDescriptor: {
    schema: 'marketDescriptorSchema',
    purpose: 'Canonical venue market metadata used by all downstream artifacts.',
    required_fields: [
      requiredField('venue', 'polymarket | kalshi', 'Venue that owns the market.'),
      requiredField('market_id', 'string', 'Stable venue-native market identifier.'),
      requiredField('question', 'string', 'Market question shown to operators and agents.'),
      requiredField('outcomes', 'string[]', 'At least two outcome labels in venue order.'),
      requiredField('active', 'boolean', 'Whether the market is currently active.'),
      requiredField('closed', 'boolean', 'Whether the market is closed for trading or resolution.'),
    ],
  },
  resolutionPolicy: {
    schema: 'resolutionPolicySchema',
    purpose: 'Eligibility and resolution guardrail for a market before advice or trading decisions.',
    required_fields: [
      requiredField('market_id', 'string', 'Market being evaluated for resolution safety.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue that defines the resolution rules.'),
      requiredField('status', 'eligible | blocked | closed | ambiguous', 'Current resolution readiness verdict.'),
      requiredField('evaluated_at', 'ISO datetime string', 'When the policy was computed.'),
    ],
  },
  marketSnapshot: {
    schema: 'marketSnapshotSchema',
    purpose: 'Canonical price and order-book snapshot used by forecasting and recommendation logic.',
    required_fields: [
      requiredField('venue', 'polymarket | kalshi', 'Venue from which the snapshot was captured.'),
      requiredField('market', 'MarketDescriptor', 'Descriptor embedded for self-contained downstream use.'),
      requiredField('captured_at', 'ISO datetime string', 'When the snapshot was taken.'),
      requiredField('yes_price', 'number | null', 'Last usable yes-side price.'),
      requiredField('no_price', 'number | null', 'Last usable no-side price.'),
      requiredField('midpoint_yes', 'number | null', 'Canonical midpoint on the yes side.'),
      requiredField('best_bid_yes', 'number | null', 'Best yes bid at capture time.'),
      requiredField('best_ask_yes', 'number | null', 'Best yes ask at capture time.'),
      requiredField('spread_bps', 'number | null', 'Spread expressed in basis points.'),
    ],
  },
  evidencePacket: {
    schema: 'evidencePacketSchema',
    purpose: 'Reusable evidence artifact backing forecast and recommendation packets.',
    required_fields: [
      requiredField('evidence_id', 'string', 'Stable evidence identifier.'),
      requiredField('market_id', 'string', 'Market to which the evidence applies.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue context for the evidence.'),
      requiredField('type', 'enum', 'Evidence classification for downstream consumers.'),
      requiredField('title', 'string', 'Short evidence label.'),
      requiredField('summary', 'string', 'Compact human-readable evidence summary.'),
      requiredField('captured_at', 'ISO datetime string', 'When the evidence was observed or generated.'),
      requiredField('content_hash', 'string', 'Integrity hash for replay and audit.'),
    ],
  },
  crossVenueMarketRef: {
    schema: 'crossVenueMarketRefSchema',
    purpose: 'Minimal reference to a venue market when building cross-venue intelligence.',
    required_fields: [
      requiredField('venue', 'polymarket | kalshi', 'Venue that owns the referenced market.'),
      requiredField('market_id', 'string', 'Stable venue-native market identifier.'),
    ],
  },
  crossVenueMatch: {
    schema: 'crossVenueMatchSchema',
    purpose: 'Read-only canonical mapping between two venue markets that may represent the same event.',
    required_fields: [
      requiredField('canonical_event_id', 'string', 'Stable cross-venue event key used for joins and audit.'),
      requiredField('left_market_ref', 'CrossVenueMarketRef', 'First venue market reference.'),
      requiredField('right_market_ref', 'CrossVenueMarketRef', 'Second venue market reference.'),
      requiredField('semantic_similarity_score', 'number 0..1', 'Question-level semantic similarity score.'),
      requiredField('resolution_compatibility_score', 'number 0..1', 'Resolution-rule compatibility score.'),
      requiredField('payout_compatibility_score', 'number 0..1', 'Payout compatibility score.'),
      requiredField('currency_compatibility_score', 'number 0..1', 'Collateral or quote-currency compatibility score.'),
    ],
  },
  venueCapabilities: {
    schema: 'venueCapabilitiesSchema',
    purpose: 'Operational capabilities and automation constraints for a venue adapter.',
    required_fields: [
      requiredField('venue', 'polymarket | kalshi', 'Venue described by the capability snapshot.'),
      requiredField('venue_type', 'execution-equivalent | reference-only | experimental', 'Classification used by guards and routing.'),
      requiredField('supports_discovery', 'boolean', 'Whether market listing is supported.'),
      requiredField('supports_orderbook', 'boolean', 'Whether usable order-book data is available.'),
      requiredField('supports_trades', 'boolean', 'Whether trades or history data are available.'),
      requiredField('supports_positions', 'boolean', 'Whether position lookup is supported.'),
      requiredField('supports_execution', 'boolean', 'Whether automation can submit orders.'),
      requiredField('supports_websocket', 'boolean', 'Whether streaming is available.'),
    ],
  },
  venueHealthSnapshot: {
    schema: 'venueHealthSnapshotSchema',
    purpose: 'Health probe summary for venue data and streaming surfaces.',
    required_fields: [
      requiredField('venue', 'polymarket | kalshi', 'Venue being monitored.'),
      requiredField('captured_at', 'ISO datetime string', 'Timestamp of the health probe.'),
      requiredField('health_score', 'number 0..1', 'Aggregate health score used by runtime guards.'),
      requiredField('api_status', 'healthy | degraded | blocked | unknown', 'API health classification.'),
      requiredField('stream_status', 'healthy | degraded | blocked | unknown', 'Streaming health classification.'),
      requiredField('staleness_ms', 'integer', 'Age of the last healthy signal or snapshot.'),
    ],
  },
  capitalLedgerSnapshot: {
    schema: 'capitalLedgerSnapshotSchema',
    purpose: 'Cash and exposure snapshot used for sizing and capital safety checks.',
    required_fields: [
      requiredField('captured_at', 'ISO datetime string', 'When the ledger view was captured.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue that owns the capital ledger.'),
      requiredField('cash_available', 'number', 'Currently available cash or collateral.'),
      requiredField('cash_locked', 'number', 'Cash locked by open positions or pending orders.'),
      requiredField('collateral_currency', 'string', 'Collateral denomination, usually USD or USDC.'),
      requiredField('open_exposure_usd', 'number', 'Current marked exposure in USD terms.'),
      requiredField('withdrawable_amount', 'number', 'Amount currently withdrawable.'),
      requiredField('transfer_latency_estimate_ms', 'integer', 'Expected transfer or settlement delay.'),
    ],
  },
  tradeIntent: {
    schema: 'tradeIntentSchema',
    purpose: 'Execution-ready intent emitted by a compliant decision path.',
    required_fields: [
      requiredField('intent_id', 'string', 'Stable trade-intent identifier.'),
      requiredField('run_id', 'string', 'Run manifest that produced the intent.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue targeted by the intent.'),
      requiredField('market_id', 'string', 'Venue-native market identifier to trade.'),
      requiredField('side', 'yes | no', 'Side to trade.'),
      requiredField('size_usd', 'number', 'Notional size in USD terms.'),
      requiredField('limit_price', 'number 0..1', 'Worst acceptable price.'),
      requiredField('max_slippage_bps', 'integer', 'Maximum tolerated slippage.'),
      requiredField('max_unhedged_leg_ms', 'integer', 'Maximum time allowed with an unhedged leg.'),
      requiredField('forecast_ref', 'string', 'Reference back to the forecast that justified the intent.'),
      requiredField('risk_checks_passed', 'boolean', 'Whether pre-trade checks passed at emission time.'),
      requiredField('created_at', 'ISO datetime string', 'When the intent was created.'),
    ],
  },
  marketRegime: {
    schema: 'marketRegimeSchema',
    purpose: 'Compact market-state classification used by strategy-layer packets and reports.',
    required_fields: [
      requiredField('label', 'string', 'Human-readable regime label.'),
      requiredField('summary', 'string', 'Short explanation of the detected regime.'),
      requiredField('confidence', 'number 0..1', 'Confidence in the regime assignment.'),
      requiredField('observed_at', 'ISO datetime string', 'When the regime snapshot was captured.'),
    ],
  },
  executionIntentPreview: {
    schema: 'executionIntentPreviewSchema',
    purpose: 'Discriminated preview of a proposed execution shape before live commitment.',
    required_fields: [
      requiredField('preview_kind', 'quote_pair | basket', 'Preview variant used to interpret the payload.'),
      requiredField('preview_id', 'string', 'Stable preview identifier.'),
      requiredField('run_id', 'string', 'Run that produced the preview.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue associated with the preview.'),
      requiredField('summary', 'string', 'Short human-readable preview summary.'),
    ],
  },
  quotePairIntentPreview: {
    schema: 'quotePairIntentPreviewSchema',
    purpose: 'Two-quote execution preview for a single prediction-market market.',
    required_fields: [
      requiredField('preview_id', 'string', 'Stable preview identifier.'),
      requiredField('run_id', 'string', 'Run that produced the preview.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue associated with the preview.'),
      requiredField('market_id', 'string', 'Market being previewed.'),
      requiredField('quotes', '[{ side, price, size_usd? } x2]', 'Exactly two quote legs for the preview.'),
      requiredField('summary', 'string', 'Short human-readable preview summary.'),
    ],
  },
  basketIntentPreview: {
    schema: 'basketIntentPreviewSchema',
    purpose: 'Multi-leg execution preview spanning one or more related markets.',
    required_fields: [
      requiredField('preview_id', 'string', 'Stable preview identifier.'),
      requiredField('run_id', 'string', 'Run that produced the preview.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue associated with the preview.'),
      requiredField('legs', '[basket leg]', 'At least one leg describing the basket composition.'),
      requiredField('summary', 'string', 'Short human-readable preview summary.'),
    ],
  },
  latencyReferenceBundle: {
    schema: 'latencyReferenceBundleSchema',
    purpose: 'Latency budget and measurement bundle used by strategy-layer execution decisions.',
    required_fields: [
      requiredField('bundle_id', 'string', 'Stable latency bundle identifier.'),
      requiredField('run_id', 'string', 'Run that produced the bundle.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue associated with the bundle.'),
      requiredField('market_id', 'string', 'Market associated with the latency measurement.'),
      requiredField('captured_at', 'ISO datetime string', 'When the latency data was captured.'),
      requiredField('summary', 'string', 'Short human-readable latency summary.'),
    ],
  },
  resolutionAnomalyReport: {
    schema: 'resolutionAnomalyReportSchema',
    purpose: 'Compact report for resolution-rule anomalies discovered by strategy analysis.',
    required_fields: [
      requiredField('report_id', 'string', 'Stable anomaly-report identifier.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue associated with the anomaly.'),
      requiredField('market_id', 'string', 'Market affected by the anomaly.'),
      requiredField('anomaly_kind', 'enum', 'High-level anomaly classification.'),
      requiredField('severity', 'low | medium | high | critical', 'Severity used for routing and review.'),
      requiredField('detected_at', 'ISO datetime string', 'When the anomaly was detected.'),
      requiredField('summary', 'string', 'Short human-readable anomaly summary.'),
    ],
  },
  autonomousAgentReport: {
    schema: 'autonomousAgentReportSchema',
    purpose: 'Agent-run report describing a strategy-layer observation or recommendation.',
    required_fields: [
      requiredField('report_id', 'string', 'Stable report identifier.'),
      requiredField('agent_id', 'string', 'Identity of the reporting agent.'),
      requiredField('agent_role', 'string', 'Role performed by the agent.'),
      requiredField('run_id', 'string', 'Run that produced the report.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue associated with the report.'),
      requiredField('market_id', 'string', 'Market covered by the report.'),
      requiredField('strategy_family', 'string', 'Primary strategy family represented by the report.'),
      requiredField('generated_at', 'ISO datetime string', 'When the report was produced.'),
      requiredField('confidence', 'number 0..1', 'Confidence in the report summary.'),
      requiredField('summary', 'string', 'Short human-readable report summary.'),
    ],
  },
  strategyShadowSummary: {
    schema: 'strategyShadowSummarySchema',
    purpose: 'Compact summary of shadow-mode strategy agreement and disagreement.',
    required_fields: [
      requiredField('shadow_id', 'string', 'Stable shadow-summary identifier.'),
      requiredField('run_id', 'string', 'Run that produced the shadow summary.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue associated with the summary.'),
      requiredField('market_id', 'string', 'Market covered by the shadow summary.'),
      requiredField('strategy_family', 'string', 'Primary strategy family under observation.'),
      requiredField('candidate_count', 'integer', 'Number of strategy candidates compared.'),
      requiredField('decision_count', 'integer', 'Number of decisions included in the summary.'),
      requiredField('disagreement_count', 'integer', 'Number of disagreement points observed.'),
      requiredField('alignment_rate', 'number 0..1', 'Agreement ratio across the shadow run.'),
      requiredField('summary', 'string', 'Short human-readable summary.'),
    ],
  },
  strategyShadowReport: {
    schema: 'strategyShadowReportSchema',
    purpose: 'Longer shadow-mode report that records how the strategy decision was evaluated.',
    required_fields: [
      requiredField('report_id', 'string', 'Stable shadow-report identifier.'),
      requiredField('shadow_id', 'string', 'Associated shadow-summary identifier.'),
      requiredField('generated_at', 'ISO datetime string', 'When the report was produced.'),
      requiredField('summary', 'string', 'Short human-readable report summary.'),
    ],
  },
  strategyCandidatePacket: {
    schema: 'strategyCandidatePacketSchema',
    purpose: 'Candidate strategy packet describing a single strategy option and its preview.',
    required_fields: [
      requiredField('packet_kind', 'strategy_candidate', 'Packet discriminator for the candidate shape.'),
      requiredField('candidate_id', 'string', 'Stable candidate identifier.'),
      requiredField('run_id', 'string', 'Run that produced the candidate.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue associated with the candidate.'),
      requiredField('market_id', 'string', 'Market covered by the candidate.'),
      requiredField('strategy_family', 'string', 'Primary strategy family associated with the candidate.'),
      requiredField('confidence', 'number 0..1', 'Confidence in the candidate.'),
      requiredField('created_at', 'ISO datetime string', 'When the candidate was produced.'),
      requiredField('summary', 'string', 'Short human-readable candidate summary.'),
    ],
  },
  strategyDecisionPacket: {
    schema: 'strategyDecisionPacketSchema',
    purpose: 'Strategy-layer decision packet that selects or rejects candidates.',
    required_fields: [
      requiredField('packet_kind', 'strategy_decision', 'Packet discriminator for the decision shape.'),
      requiredField('decision_id', 'string', 'Stable decision identifier.'),
      requiredField('run_id', 'string', 'Run that produced the decision.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue associated with the decision.'),
      requiredField('market_id', 'string', 'Market covered by the decision.'),
      requiredField('decision', 'adopt | reject | shadow | defer', 'Decision outcome emitted by the strategy layer.'),
      requiredField('confidence', 'number 0..1', 'Confidence in the decision.'),
      requiredField('created_at', 'ISO datetime string', 'When the decision was produced.'),
      requiredField('summary', 'string', 'Short human-readable decision summary.'),
    ],
  },
  predictionMarketPacketBundle: {
    schema: 'predictionMarketPacketBundleSchema',
    purpose: 'Top-level packet bundle that can now carry strategy candidate and decision data.',
    required_fields: [
      requiredField('bundle_id', 'string', 'Stable bundle identifier.'),
      requiredField('run_id', 'string', 'Run that owns the bundle.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue associated with the bundle.'),
      requiredField('market_id', 'string', 'Market covered by the bundle.'),
    ],
  },
  forecastPacket: {
    schema: 'forecastPacketSchema',
    purpose: 'Canonical probability forecast for a single market.',
    required_fields: [
      requiredField('market_id', 'string', 'Market covered by the forecast.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue that owns the market.'),
      requiredField('basis', 'market_midpoint | manual_thesis', 'Primary basis used for the forecast.'),
      requiredField('probability_yes', 'number 0..1', 'Forecasted yes probability.'),
      requiredField('confidence', 'number 0..1', 'Model confidence in the forecast.'),
      requiredField('rationale', 'string', 'Human-readable explanation of the forecast.'),
      requiredField('produced_at', 'ISO datetime string', 'When the forecast was emitted.'),
    ],
  },
  marketRecommendationPacket: {
    schema: 'marketRecommendationPacketSchema',
    purpose: 'Operator-facing recommendation derived from snapshot and forecast state.',
    required_fields: [
      requiredField('market_id', 'string', 'Market covered by the recommendation.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue that owns the market.'),
      requiredField('action', 'bet | no_trade | wait', 'Recommended action.'),
      requiredField('side', 'yes | no | null', 'Recommended side when action implies a trade.'),
      requiredField('confidence', 'number 0..1', 'Confidence in the recommendation.'),
      requiredField('fair_value_yes', 'number 0..1', 'Estimated fair yes price.'),
      requiredField('market_price_yes', 'number | null', 'Observed market yes price.'),
      requiredField('market_bid_yes', 'number | null', 'Observed market yes bid.'),
      requiredField('market_ask_yes', 'number | null', 'Observed market yes ask.'),
      requiredField('edge_bps', 'number', 'Estimated edge in basis points.'),
      requiredField('spread_bps', 'number | null', 'Observed spread in basis points.'),
      requiredField('produced_at', 'ISO datetime string', 'When the recommendation was produced.'),
    ],
  },
  predictionMarketArtifactRef: {
    schema: 'predictionMarketArtifactRefSchema',
    purpose: 'Compact artifact pointer embedded in manifests and persisted run summaries.',
    required_fields: [
      requiredField('artifact_id', 'string', 'Stable artifact identifier.'),
      requiredField('artifact_type', 'artifact enum', 'Artifact classification used by consumers and audit tooling.'),
      requiredField('sha256', 'string', 'Integrity hash of the artifact payload.'),
    ],
  },
  runManifest: {
    schema: 'runManifestSchema',
    purpose: 'Audit and lineage root for a prediction-markets run.',
    required_fields: [
      requiredField('run_id', 'string', 'Stable run identifier.'),
      requiredField('mode', 'advise | replay', 'Run mode executed by the system.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue targeted by the run.'),
      requiredField('market_id', 'string', 'Market evaluated by the run.'),
      requiredField('started_at', 'ISO datetime string', 'Run start time.'),
      requiredField('status', 'running | completed | failed', 'Final or current run status.'),
      requiredField('config_hash', 'string', 'Hash of the effective runtime configuration.'),
    ],
  },
  predictionMarketRunSummary: {
    schema: 'predictionMarketRunSummarySchema',
    purpose: 'Compact persisted read model for operators and APIs.',
    required_fields: [
      requiredField('run_id', 'string', 'Stable run identifier.'),
      requiredField('workspace_id', 'integer', 'Owning workspace identifier.'),
      requiredField('venue', 'polymarket | kalshi', 'Venue targeted by the run.'),
      requiredField('mode', 'advise | replay', 'Run mode stored in the summary.'),
      requiredField('market_id', 'string', 'Market evaluated by the run.'),
      requiredField('status', 'running | completed | failed', 'Stored run status.'),
      requiredField('created_at', 'integer', 'Creation timestamp in epoch seconds.'),
      requiredField('updated_at', 'integer', 'Update timestamp in epoch seconds.'),
      requiredField('manifest', 'RunManifest', 'Embedded manifest for lineage and audit.'),
    ],
  },
  predictionMarketsAdviceRequest: {
    schema: 'predictionMarketsAdviceRequestSchema',
    purpose: 'Minimal API request for an advice run on a single market.',
    required_fields: [
      requiredField('market_id', 'string', 'Venue market ID. This may be replaced by slug in user-facing flows.'),
    ],
  },
  predictionMarketsReplayRequest: {
    schema: 'predictionMarketsReplayRequestSchema',
    purpose: 'Minimal API request for replaying a previously recorded run.',
    required_fields: [
      requiredField('run_id', 'string', 'Existing run identifier to replay.'),
    ],
  },
}
