# swarm-ui

Copie directe du frontend MiroShark pour le dashboard Swarm.

## Bootstrap

```bash
cd /home/jul/swarm/dashboard/swarm-ui
npm install
npm run dev
```

Le proxy Vite pointe sur `http://localhost:5001` pour les routes `/api`.
